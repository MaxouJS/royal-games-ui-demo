"use client";

import { Panel } from "@objectifthunes/royal-games-ui";

export function Demo() {
  return (
    <div style={{ color: "#bcb0de", font: "700 13px/1.5 var(--rg-font-body)", maxWidth: 420 }}>
      Body is the scrolling content column between the chrome and the docks, it spaces
      its children and reserves bottom padding for the BottomStack. See any screen example.
    </div>
  );
}
